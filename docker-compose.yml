version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: signature-strokes
    ports:
      - "8002:8002"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
